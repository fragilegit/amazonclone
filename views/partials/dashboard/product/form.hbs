<div class="col-xs-12 col-md-8 col-lg-9">
    <div class="box box-success">
        <div class="box-header">
            <div class="pull-left">
                {{!-- @if (Session::has('success'))
                        <div class="alert alert-success">{{ Session::get('success') }}</div>
                    @elseif (Session::has('warnning'))
                        <div class="alert alert-danger">{{ Session::get('warnning') }}</div>
                    @endif --}}
            </div>
        </div>
        <!-- /.box-header -->
        <div class="box-body ">
            <div class="form-group">
                <label for="">Product Name</label>
                <div class="input-group">
                    <div class="input-group-addon">
                        <input type="checkbox" name="bestseller" {{#if product.bestseller}}checked{{/if}}>
                    </div>
                    <input type="text" class="form-control" name="name"
                        value="{{#if product.name}}{{product.name}}{{/if}}"
                        placeholder="Enter product name & select checkbox if bestseller" required>
                </div>

            </div>
            <div class="form-group">
                <label for="">Slug</label>
                <input type="text" class="form-control" name="slug" value="{{#if product.name}}{{product.name}}{{/if}}"
                    required>
            </div>
            <div class="form-group">
                <label for="">Product Price</label>
                <input type="number" class="form-control" name="price"
                    value="{{#if product.price}}{{product.price}}{{/if}}" required>
            </div>
            <div class="form-group">
                <label for="">Description</label>
                <input type="text" class="form-control" name="description" value="{{#if product.description}}{{product.description}}{{/if}}" required>
            </div>
            {{!-- <div class="form-group">
                <label for="">Quantity</label>
                <input type="number" class="form-control" name="quantity"
                    value="{{#if product.quantity}}{{product.quantity}}{{/if}}" required>
            </div> --}}
            <hr>
        </div>
        <!-- /.box-body -->
    </div>
    <!-- /.box -->
</div>
<div class="col-xs-12 col-md-4 col-lg-3">
    <div class="box">
        <div class="box-header">
            <h2 class="box-title">Product Date</h2>
        </div>
        <div class="box-body">
            <div class="form-group">
                <div class='input-group date' id='datetimepicker1' data-provide="datepicker">
                    <input type="text" class="form-control" name="published_at" placeholder="Y-m-d H:i:s"
                        value="{{#if product.createdAt}}{{product.createdAt}}{{/if}}" required>
                    <span class="input-group-addon">
                        <span class="glyphicon glyphicon-calendar"></span>
                    </span>
                </div>

                {{!-- {!! $errors->first('published_at', '<p class="help-block">:message</p>') !!} --}}
            </div>
        </div>
        <div class="box-footer">
            <div class="pull-left">
                <input type="submit" class="btn btn-default" id="draft-btn" name="product_option" value="Save Draft">
            </div>
            <div class="pull-right">
                <input type="submit" class="btn btn-primary" id="draft-btn" name="product_option" value="Publish">
            </div>
        </div>
        {{!-- /.box-body  --}}
    </div>
    {{!-- /.box --}}
    <div class="box">
        <div class="box-header width-border">
            <h2 class="box-title">Category</h2>
        </div>
        <div class="box-body">
            <div class="form-group">
                <select name="category" class="form-control" id="">
                    <option value="">Select Category</option>

                    {{#each categories}}
                    {{#ifCond this.id ../product.category }}
                            <option value="{{this.id}}" selected="selected">{{this.title}}</option>
                        {{/ifCond}}
                        <option value="{{this.id}}">{{this.title}} {{../product.category}}</option>
                    {{/each}}
                </select>
            </div>
        </div>
    </div>
    {{!-- <div class="box">
        <div class="box-header width-border">
            <div class="box-title">Tags</div>
        </div>
        <div class="box-body">
            <div class="form-group">
                {!! Form::text('event_tags', null,['class' => 'form-control']) !!}
            </div>
        </div>
    </div> --}}
    <div class="box">
        <div class="box-header">
            <h3 class="box-title with-border">Feature Image</h3>
        </div>
        <div class="box-body text-center">
            <div class="form-group">
                <br>
                <div class="fileinput fileinput-new" data-provides="fileinput">
                    <div class="fileinput-new thumbnail" style="width: 200px; height: 150px;">
                        <img src="{{#if product.image}}/uploads/{{product.image}}{{else}}https://placehold.it/200x150$text=No+Image{{/if}}"
                            alt="...">
                        {{!-- <img src="/storage/event_image/{{ $event->event_image }}" alt="..."> --}}
                    </div>
                    <div class="fileinput-preview fileinput-exists thumbnail"
                        style="max-width: 200px; max-height: 150px;"></div>
                    <div>
                        <span class="btn btn-default btn-file">
                            <span class="fileinput-new">Select image</span>
                            <span class="fileinput-exists">Change</span>
                            <input type="file" id="exampleInputFile" name="image">
                        </span>
                        <a href="#" class="btn btn-default fileinput-exists" data-dismiss="fileinput">Remove</a>
                    </div>
                </div>
                {{!-- {!! $errors->first('event_image', '<p class="help-block">:message</p>') !!} --}}
            </div>
        </div>
    </div>
    {{!-- /.box --}}
</div>